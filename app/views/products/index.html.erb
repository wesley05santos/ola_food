<%=link_to 'Novo Produto', new_product_path %>

<% @products.each do |product| %>
  <p>
    <%= product.code %> |
    <%= product.name %> |
    <%= product.price %> =>
    <%= link_to  "Detalhes", product %>
  </p>
<% end %>